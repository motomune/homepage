# homepage

3D演出付きのランディングページ。Vite + Three.js。GitHub Pagesでの公開を想定した静的サイト。

## 開発

```bash
npm install
npm run dev
```

## ビルド

```bash
npm run build
```

`dist/` に静的ファイルが出力されます。GitHub Pages にはこの `dist/` の中身をデプロイしてください。
